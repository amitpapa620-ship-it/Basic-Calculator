🧮 Basic Calculator (Vanilla JS + DSA)

A web-based calculator built using Vanilla JavaScript, HTML, and CSS.
This calculator evaluates mathematical expressions using Data Structures & Algorithms (Stacks) instead of JavaScript’s eval() function.

⚠️ No eval() is used — expressions are parsed and evaluated manually for safety and correctness.

✨ Features

➕ Addition

➖ Subtraction

✖ Multiplication

➗ Division

📊 Percentage (%)

🧠 Operator precedence handling

📚 Stack-based expression evaluation

🔁 Implicit multiplication (e.g. 2(3+4))

➖ Unary minus support (e.g. -5+3)

⌨ Keyboard input support

🔙 Backspace & Clear

🛠️ Technologies Used

HTML – UI structure

CSS – Styling

Vanilla JavaScript – Logic & DSA-based evaluation

🧠 How It Works (DSA Approach)

This calculator uses:

Two stacks

values → stores numbers

operators → stores operators

Operator precedence

Infix expression parsing

Manual evaluation algorithm

This approach ensures:

Better security (no code execution)

Correct operator precedence

Interview-ready implementation

🚀 How to Run the Project
Run Locally

Clone the repository:

git clone https://github.com/your-username/your-repo-name.git


Open the project folder

Open index.html in any browser

⌨ Keyboard Controls
Key	Action
0–9	Numbers
+ - * /	Operators
%	Percentage
Enter	Calculate
Backspace	Delete
Esc	Clear
